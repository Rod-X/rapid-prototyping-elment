import { mount } from '@vue/test-utils'
import Elment from '../src/link.vue'

describe('rod-Link',()=>{
    // test('button-text',()=>{
        // const wrapper = mount(Elment)
        // expect(wrapper.html()).toContain('')
    // })
})
